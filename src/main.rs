mod libs;

#[allow(unused_imports)]
use libs::tokens;

fn main() {}

#[cfg(test)]
mod test {
    #[allow(unused_imports)]
    use super::*;

    #[test]
    fn sanity_tests() {
        assert!(true);
    }
}
